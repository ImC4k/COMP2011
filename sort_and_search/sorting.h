#ifndef _SORTING_H
#define _SORTING_H
#include <iostream>
using namespace std;

void bubble_sort(double*, int);           // parse in a double array pointer, and the size of the array to sort

bool binary_search(double*, int, double); // parse in a double array pointer, size of array and value wanted to search

void print_array(double*, int);           // parse in a double array pointer, and size of array for printing

#endif
